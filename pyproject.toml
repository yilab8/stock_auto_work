[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "kham-ticketbot"
version = "0.1.0"
description = "Automation toolkit for KHAM ticketing website"
authors = [{name = "AutoGenerated"}]
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
ticketbot = "ticketbot.cli:main"
ticketbot-gui = "ticketbot.gui:run_gui"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = [
    "tests",
]
